# Rules

Read and follow all markdown files in `agent-rules/rules/`.

# Commands

Read and follow command definitions in `agent-rules/commands/`. Each markdown file defines a slash command named after the file (e.g., `commit.md` defines `/commit`).
